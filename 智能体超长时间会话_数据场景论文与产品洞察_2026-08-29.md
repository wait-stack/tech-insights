# 智能体超长时间会话：数据、场景、论文与产品技术洞察

> **报告日期**：2026-08-29<br>
> **增量范围**：重点覆盖 2026-08-04—2026-08-29，并与仓库内的[上一期报告](./超长轮次Agent记忆最新技术洞察报告_2026-08.md)衔接<br>
> **研究对象**：跨上下文、跨会话、跨任务或持续数小时至数月的智能体运行，不局限于聊天记忆<br>
> **证据来源**：arXiv 论文全文、论文代码/数据说明、厂商官方文档与发布记录<br>
> **阅读提示**：不同论文使用的模型、提示、读预算、数据子集和评分器不同，本文只在同一论文的受控实验内比较数字，不制作跨论文“统一排行榜”

---

## 一、结论先行

### 1.1 领域定义正在变化

“超长时间会话”不再等于把更多聊天记录塞进更长的上下文。最新研究正在把它重新定义为一个**持续运行、持续变化、可中断恢复、可被监督的计算过程**。需要长期保存的至少有四类对象：

1. **历史（History）**：发生过什么，要求原始证据、时间、来源可追溯；
2. **状态（State）**：现在是什么，要求结构化、可校验、可事务更新；
3. **记忆（Memory）**：过去有哪些事实、经验、偏好和规则值得复用，要求生命周期治理；
4. **活跃上下文（Working View）**：本次模型调用真正需要看什么，要求按问题、预算和模态动态组装。

这四者不能继续共用一个“不断增长的消息列表”。截至 2026 年 8 月底，论文与产品已经共同收敛到下面的分层：

```text
不可变事件日志 / 原始多模态归档
              ↓ 派生、校验、带来源
当前执行状态 + 长期记忆 + 分支/时间关系
              ↓ 按当前问题与预算选择
       有界的模型工作视图
              ↓ 行动、观察、检查点
      Agent 执行 + 人/监督智能体实时干预
```

### 1.2 本轮最重要的六个变化

| 变化 | 过去常见做法 | 2026-08 新证据 | 工程含义 |
|---|---|---|---|
| 评测从“能答”转向“会用” | 直接提问历史事实 | MemUse 显示 Direct QA 与满意度脱钩 | 记忆召回率不能代表真实用户价值 |
| 会话从文本历史转向显式状态 | 追加 ReAct 轨迹 | SKILL.state 在 200 步保持约 1.8K 平均提示 | 执行状态应成为运行时一等对象 |
| 原始历史从摘要替代转向可恢复事件日志 | 压缩后丢弃原文 | Scroll 在 10M-token 历史上保留可寻址原始证据 | 摘要应是视图，不应是唯一事实源 |
| 检索从“相似 Top-k”转向“恢复当前交互状态” | 全局向量召回 | ArborMem 先定位被恢复的对话分支 | 长会话首先是轨迹续接问题，其次才是知识检索 |
| 经验记忆从“写进去”转向“治理一生” | 成功轨迹直接沉淀 | MemGuard 持久化 verifier 信号管理准入、冲突、归档 | 相关但错误的记忆比召回不到更危险 |
| 人机协作从回合后复盘转向运行中转向 | 任务结束后 reflection | PILOT、Claude Managed Agents、Codex 远程控制支持实时 steering | 长任务需要中断、转向和恢复，而不是只等最终结果 |

### 1.3 最关键的判断

> **未来超长会话系统的竞争点，不是“能存多少”，而是能否把原始证据、当前状态、长期记忆和活跃上下文分开管理，并在数百步、数月或多模态历史中保持可恢复、可验证、可干预。**

---

## 二、数据与场景的真实特征

### 2.1 “长”至少有六个互相独立的维度

| 维度 | 代表数据 | 真正压力 | 仅扩大上下文为何不够 |
|---|---|---|---|
| 自然时间 | MemUse：约 4 个月；FM-Bench：20 个模拟年 | 偏好变化、过期、长期因果 | 时间跨度不等于 token 连续出现 |
| 决策步数 | FM-Bench：约 340–400 个决策点；SKILL.state：10–200 步 | 错误累积、目标漂移、恢复 | 每步重放历史导致累计成本近二次增长 |
| 历史体量 | LongMemEval M：约 1.5M tokens；BEAM：最高 10M tokens | 稀疏证据、多跳、聚合 | 名义窗口远小于可靠利用窗口 |
| 任务流长度 | MemGuard：最长 2,294 个 SWE-bench issue 连续流 | 经验污染、重复、陈旧、冲突 | 历史任务不能全部直接注入当前任务 |
| 交互拓扑 | BranchMemEval：每会话 3–4 个交错分支 | 中断后续接、同名实体混淆 | 相似度无法判断“当前恢复哪条线” |
| 模态保真度 | ATM、Mem-Gallery、MemEye、H2HMem、GUI 历史截图 | 图片细节、OCR、状态变化 | 文本摘要无法保留所有未来所需像素证据 |

因此，报告中的“超长时间会话”应按下面的向量理解，而不是按单一 token 数理解：

```text
Horizon = 时间跨度 × 决策步数 × 历史体量 × 任务数量 × 分支复杂度 × 模态密度
```

### 2.2 数据并不是均匀有用的流，而是五种不同对象的混合

1. **精确约束**：安全规则、权限、合同限制、用户明确禁忌。它们对措辞和否定词高度敏感，不能和日志一起自由摘要。
2. **可变状态**：当前库存、文件、任务进度、账户状态、用户当前工作单位。它们需要覆盖旧值，但又必须保留版本和来源。
3. **稳定语义**：长期偏好、实体关系、项目约定。它们适合结构化、链接和跨会话复用。
4. **情节与证据**：一次对话、一段工具输出、一张截图。它们大多无需常驻上下文，但必须可按地址恢复。
5. **程序与经验**：解决步骤、失败模式、工具使用规律。它们应经过结果验证后再跨任务复用。

[Compaction Cliff](https://arxiv.org/abs/2608.22752) 对 396,934 份、来自 54,628 个公开 GitHub 仓库的 Agent 配置做了分析，并把内容归为 Constraint、Procedural、Belief、Preference、Episodic 五类。论文在 20 个生产 Agent 配置上复现实验：Claude Code 的 `/compact` 提示在 Sonnet 4.6 上，一轮后只保留 53% 安全规则，五轮后只剩 10%。这说明**数据类型决定允许的失真率**，统一摘要策略在结构上就不安全。

### 2.3 真正需要记忆的时刻非常稀疏

[MemUse](https://arxiv.org/abs/2608.24189) 是本轮最重要的数据发现：

- 40 名用户与 AI 日记伙伴持续约 4 个月，共 1,872 个会话、21,575 个 turn；
- 研究者从 10,795 个用户 turn 中人工核验出 147 个记忆相关事件；
- 用户自然触发记忆的频率只有 **1.4% 的用户 turn**，约每 73 个用户 turn 一次；
- 最终可复现实验的 MemUse 包含 72 个真实时刻、316 个事实问题。

这和主流基准 15%–24% 的“每若干轮就问一次历史事实”完全不同。生产系统的难点不是每轮都检索，而是：

1. 识别这 1.4% 的时刻；
2. 判断用户是在测试记忆、补充背景，还是自然承接旧话题；
3. 用得自然，不强行炫耀记忆；
4. 避免在错误时机主动回忆。

论文中，Direct QA 正确率在七种记忆条件间从 19.7% 增长到 70.1%，但用户满意度不变。同一模型、同一上下文，在 LC-100% 条件下能直接回答 78.8% 的事实，却只在自然回复中引用 7.9%，相差 71 个百分点。Natural Integration 与满意度相关（Spearman ρ=0.29，p=.046），Direct QA 则几乎无关（ρ=0.03）。

**推论**：生产系统应把“是否需要使用记忆”作为独立决策，而不是把召回结果无条件塞进每次生成。

### 2.4 长任务不是长问答，而是动态环境中的状态机

[SKILL.state](https://arxiv.org/abs/2608.26263) 的两个合成环境很好地揭示了数据形态：

- 仓库管理有 500 个独立货架，动作会改变库存状态；
- 软件仓库环境包含分支、commit、PR 和 CI 状态的依赖图，一次 merge 会改变后续多个对象；
- 高噪声条件每一步可加入 50 个遥测、OCR 或系统日志干扰项；
- 外部世界还会静默改变真实状态，测试智能体需要几步才能恢复。

这类数据的核心不是“过去说过什么”，而是“世界当前是什么”。如果仍保留旧观察作为平级文本，模型会被历史事实压过新证据。SKILL.state 用经过校验的结构化 state patch 替代完整轨迹，在 200 步时保持 0.94 准确率、平均提示约 1,811 tokens；摘要式基线使用约 6.18M 累计 tokens，准确率 0.84。外部状态纠正后，历史式基线需要 5–8 步恢复，SKILL.state 为 0 步。

### 2.5 长会话天然是多分支、可中断、可恢复的

[ArborMem](https://arxiv.org/abs/2608.17534) 指出语义相关不等于会话连续：同一个人可以同时推进旅行、健康和项目三个主题，某一轮是在恢复一条被中断的轨迹，而不是搜索所有相关事实。

它构造的 BranchMemEval 有：

- 33 个会话、100 个在线问题、1,442 个非问题交互；
- 每个会话 3–4 个交错主题分支、42–52 轮，平均 46.73 轮；
- 每个问题都有一个结构上可信但属于错误分支的“混淆答案”；
- 评测逐轮回放，只允许访问问题前已提交的历史，并用规则评分避免 LLM judge 偏差。

在 256–1K token 的极小读预算下，ArborMem 相比最强竞争方法高 16–26 个百分点。消融中移除 state localization，30B 模型在 50 道 LongMemEval 子集上从 82% 降到 70%。这说明**先恢复正确分支，再补充跨分支事实**，比全局相似检索更符合超长会话结构。

### 2.6 长期场景必须包含延迟收益、隐藏信息和竞争反馈

[FM-Bench](https://arxiv.org/abs/2608.18423) 让智能体经营足球俱乐部 20 个模拟年，通过 26 个工具完成约 340–400 个决策点。其场景同时包含：

- 隐藏信息：球员真实能力和潜力不可见，侦察带永久偏差；
- 累积后果：青训、设施、现金管理的收益跨多年出现；
- 反应型环境：对手会争抢同一球员，重复出价会改变市场；
- 多目标压力：成绩、财务和董事会信心同时约束；
- 自主管理记忆：每个决策点都是新会话，只携带智能体自己写的 notebook。

15 个前沿模型都能跑完 20 年，但 token 消耗与成绩无相关性；没有模型从数百次被拒报价中学会隐藏市场价格。自管 notebook 出现两个相反失败模式：**只增不减的档案**和**每季整体重写的计划**。这是当前记忆工程的缩影：追加导致噪声，整体覆盖导致历史和依据丢失。

### 2.7 多模态长期记忆的瓶颈是“恢复哪一段高保真证据”

[CausalCache](https://arxiv.org/abs/2608.22577) 假设全部历史事件都保留文本摘要，但只有固定数量 `B` 的事件能恢复原始截图。其主实验在 `B=4` 下比较“最近四张”与“预测最有用的四张”：

- OSWorld-Verified 361 个任务，15 步限制下两者几乎持平；延长到 30 步后 CausalCache 为 46.7%，Recent-4 为 42.4%；
- MobileWorld 117 个任务中，62 个跨应用记忆任务从 19.4% 提升到 30.6%，55 个单应用对照仅从 42.4% 到 43.6%；
- 恢复历史截图相对纯摘要在桌面任务上总体提升约 13 个百分点。

这表明多模态记忆不能只做“图片向量检索”。系统要保留低成本摘要索引，同时按当前动作的因果需要恢复少量原始截图、音频或文件片段。

---

## 三、2026-08-04 以来重点论文技术路线

### 3.1 十篇重点深读

#### 1. [MemUse](https://arxiv.org/abs/2608.24189)：把记忆评测从 Direct QA 推向 Natural Integration

- **问题**：基准上的历史问答准确率是否真的提高长期用户满意度？
- **方法**：4 个月真实部署，随机轮换 Summary、LC-10/50/100%、RAG-10/50/100% 七种条件；从真实对话中检测用户自然触发的记忆时刻。
- **结果**：Direct QA 随容量明显提高但满意度不变；Natural Integration 与满意度相关；主动回忆若时机错误，满意度约下降 0.48 个用户内标准差。
- **限制**：只有 40 名用户，Natural Integration 的二元 judge 对“像是在回忆”的措辞有一定脆弱性；论文也主动报告了这一点。
- **启示**：新增 `memory_need_detection` 与 `integration_policy` 两层评测，不能继续把“检索到”视为“使用成功”。

#### 2. [SKILL.state](https://arxiv.org/abs/2608.26263)：把执行状态从对话文本中剥离

- **问题**：如何让 100–200 步程序型智能体避免上下文二次增长和旧事实污染？
- **方法**：每步只给模型“不可变技能说明 + 当前结构化状态 + 最新观察”，模型输出动作与 state patch，运行时校验后合并；中间推理立即丢弃。
- **结果**：Warehouse 100 步时，相比 Stateful 基线累计 token 下降 16.2 倍；200 步仍维持 0.94 准确率。InterCode CTF 达 54.2%，比最强基线高 7.8 点，并减少 60% 以上 token。
- **限制**：前提是领域存在可定义的充分状态；对于开放研究、审计或未知 schema 任务，历史仍不可替代；小模型主要失败于 patch 覆盖和类型错误。
- **启示**：状态存储不是“另一种记忆”，而是与记忆检索并列的运行时基础设施。

#### 3. [Scroll](https://arxiv.org/abs/2608.21690)：把整个会话变成可执行环境

- **问题**：写入时摘要不知道未来需要什么，如何避免不可逆信息丢失？
- **方法**：SQLite append-only Event Log 保存带稳定地址的原始事件，大 payload 外置；持久 Python kernel 保存 typed namespace；模型用 `search/expand/exec` 操作历史，只有 `print` 的投影进入下一轮上下文；被移出的内容由分层 eviction index 指向原始地址。
- **结果**：Qwen3.8-Max 在 LongMemEval-S 为 94.8%，BEAM-10M 为 73.1%，LOCA-256K 为 86.7%；BEAM-10M 每题中位输入约 105K tokens，仅为语料的约 1%。丢弃原始记录的摘要消融在 BEAM-10M 从 73.1 跌到 19.9。
- **限制**：效果高度依赖模型的代码生成、查询规划和停止判断；在多会话聚合、总结、偏好抽取上仍弱于某些预计算系统。
- **启示**：原始历史应是可计算的数据层，RAG/摘要只是其上的缓存或索引。

#### 4. [ArborMem](https://arxiv.org/abs/2608.17534)：将对话组织为可导航的交互状态森林

- **问题**：相似检索会把结构上不同但语义接近的任务线混在一起。
- **方法**：新输入先定位到一个既有或新建 interaction state，恢复该分支的局部轨迹，再跨分支补充原子事实和结构化记录。
- **结果**：在 LongMemEval、LoCoMo、BEAM-100K、BranchMemEval 上分别超过最强基线 9.00、10.31、3.36、5.00 点；完整查询延迟低于 0.5 秒。
- **限制**：单父节点森林不适合多意图轮次；早期路由错误会传播；构建成本仍依赖模型抽取。
- **启示**：会话路由器需要输出“恢复哪条任务线”，而不仅是主题标签。

#### 5. [Compaction Cliff / Knowledge Triage](https://arxiv.org/abs/2608.22752)：按类型分配失真预算

- **问题**：统一摘要会让安全约束与普通日志以相同概率丢失。
- **方法**：把知识分为约束、程序、信念、偏好、情节五类；TypeCompact 对约束原样保留，TypeDecompose 将适用约束复制到每个分区，TypeRetrieve 在相关性排名前固定注入适用规则。
- **结果**：五轮压缩约束召回 96%，统一方法仅 10%；分区局部性违规 0% 对比 93%；检索 Recall@50 达 100% 对比最佳单次 LLM 检索器 73%。
- **限制**：保证完全依赖分类器召回；默认 SafetyMargin 约束召回约 0.93，声明式安全文本仍会漏；复制约束的最坏开销可达 219%。
- **启示**：所有 compaction 前先做 typed triage；约束必须 exact-preserve，不能让摘要模型自行决定。

#### 6. [MemGuard](https://arxiv.org/abs/2608.21867)：把 verifier 结果变成记忆生命周期元数据

- **问题**：失败轨迹、偶然成功和过期经验会被错误沉淀，随后在长任务流中持续污染。
- **方法**：对任务完成、证据一致性、执行有效性、泛化性评分，得到 reward、confidence、label、uncertainty；这些字段不只用于一次准入，还参与检索、冲突处理、合并、摘要和归档。失败经验以 guard 而不是动作模板保存。
- **结果**：在 Terminal-Bench 2.0、SWE-Bench Verified、WebArena、Mind2Web × 四个 backbone 的 16 组设置中全部最好；相对 ReasoningBank 最大提升 WebArena 7.9 点、Mind2Web 5.6 点。移除治理是最大消融损失。
- **限制**：verifier 与人工判断一致率约 83%–86%，仍可能把错误经验升为 active；未覆盖多模态、隐私和高风险生产环境。
- **启示**：记忆记录至少需要 `state/quality/confidence/source/validity/reuse_count/conflict_links`，而不只是文本和 embedding。

#### 7. [PILOT](https://arxiv.org/abs/2608.26530)：运行中监督与跨任务自我演化合并

- **问题**：单智能体自省被执行细节淹没，而传统子智能体通常只能在结束后返回结果，无法中途纠偏。
- **方法**：supervisor 与 worker 独立上下文，通过 notification、question、result、steer、abort 五种事件连接；监督中发现的程序和失败模式实时写入技能/记忆，后续 worker 立即加载。
- **结果**：两种冻结 backbone、三个长程基准的六组设置中五组第一；Terminal-Bench 2.0 达 71.6% 平均通过率；迭代自改进提高 12.4–14.6 点，输出 token 下降 42.9%–47.4%。
- **限制**：只有两个开放模型；自改进需要多轮重复任务，成本高；成功任务的更新才进入下一轮，仍依赖外部结果筛选。
- **启示**：超长任务的控制面应独立于执行上下文，并支持异步事件、实时 steering 和 abort。

#### 8. [GraphMemix](https://arxiv.org/abs/2608.26983)：多模态记忆从 Top-k 变成证据森林优化

- **问题**：相似 Top-k 既会重复取近似图片，也会漏掉只有与锚点组合后才有价值的低相似证据。
- **方法**：多视图种子检索后沿 schema/语义边扩展候选图；分别用 node verifier 判断直接支持、Evidence-Chain Verifier 判断相对锚点的增量价值；在最多 10 条证据预算下优化最大权森林。
- **结果**：四个多模态长期记忆基准、Qwen3-VL reader 的平均 judge accuracy 为 61.55%，比最强公开基线高 11.75 点；ATM 全生命周期耗时比 A-MEM、VimRAG、LightMem 分别短约 1.78、4.27、4.74 倍。
- **限制**：H2HMem 仍有 50.46% 金标准证据无法进入候选图；主要指标依赖 GPT-5-mini judge；每题需要两个 verifier 调用。
- **启示**：多模态召回应优化“证据集合的互补性”，而非独立打分后截断。

#### 9. [CausalCache](https://arxiv.org/abs/2608.22577)：在固定视觉预算下恢复历史像素

- **问题**：所有历史截图无法常驻，但纯文本摘要又丢失视觉细节。
- **方法**：完整历史保留文本摘要；selector 从全轨迹中选择 `B` 个事件恢复截图；history-gated KV adapter 只作用于恢复的历史图像 token。
- **结果**：跨应用移动任务在相同四图预算下提高 11.2 点；延长桌面任务 horizon 后提高 4.3 点；单应用对照无显著变化，证明收益集中在真正需要远距视觉证据的场景。
- **限制**：第二次模型调用在 81%–87% 移动步骤触发，增加约 8%–10.5% 每步时间；目前只验证截图，不覆盖音频和连续视频。
- **启示**：多模态长期记忆需要“低保真常驻 + 高保真按需恢复”的分级存储。

#### 10. [FM-Bench](https://arxiv.org/abs/2608.18423)：把超长智能体评测推进到管理行为

- **问题**：短任务不能测出延迟收益、长期信用分配、竞争适应和终局意识。
- **方法**：20 年、16 个竞争席位、确定性引擎、无 LLM judge；最终分数拆成六类管理行为，并保留可重放日志。
- **结果**：模型规模、价格、token 花费均不能解释排名；高分模型更早续约、减少期末慢回报投资、少持有闲置现金；没有模型学会隐藏市场价格。
- **限制**：游戏环境仍是单一抽象领域；solo 只有三个 seed，Arena 只有一个共享世界；行为相关不等于因果。
- **启示**：超长评测必须让早期动作改变后期环境，并测行为曲线而非只测最终答案。

### 3.2 其他值得跟踪的增量论文

| 日期 | 论文 | 主要信号 |
|---|---|---|
| 08-05 | [HiGram](https://arxiv.org/abs/2608.05095) | 分层图记忆 + path-level 定位与联合重写，关注动态冲突下的依赖更新 |
| 08-08 | [Mitigating Over-Personalization via Structured Memory](https://arxiv.org/abs/2608.08300) | 按领域分组注入记忆，平均减少 8.8% 跨域泄漏，提醒“记得太多”也会伤害回答 |
| 08-09 | [Private Etymology](https://arxiv.org/abs/2608.08443) | 长期人机关系中的共享符号需要协商、修订、遗忘和退休，而不是固定事实值 |
| 08-12 | [EgoCITE](https://arxiv.org/abs/2608.12627) | 第一视角长视频以多粒度、多视图、时间意图感知检索组织，成本比长上下文低 36 倍 |
| 08-13 | [LycheeMemory V2](https://arxiv.org/abs/2608.12990) | 从逐 turn 抽取改为语义段合并，LongMemEval-S 92.20%，构建 token 比 A-MEM 低 75.9% |
| 08-17 | [SP-Mem](https://arxiv.org/abs/2608.16551) | 敏感值与可用语义分离，按任务需要和同意状态选择性恢复私密字段 |
| 08-18 | [CABLE](https://arxiv.org/abs/2608.17911) | 建立与宿主 retriever 互补的前因链接，而不是重复语义近邻 |
| 08-18 | [AdaLens](https://arxiv.org/abs/2608.17834) | 用 storyline 展示并操控长时间数据分析的分支、证据和进度 |
| 08-22 | [ECHO](https://arxiv.org/abs/2608.21755) | 可审计记忆平面主动披露负结果与查询扩展泄漏，强调开发集高分不等于独立验证 |
| 08-24 | [EARM](https://arxiv.org/abs/2608.22767) | retriever 也应记住过去的 query-memory 相关性评分，把 rerank 成本跨生命周期摊销 |
| 08-24 | [MobilePA-Bench](https://arxiv.org/abs/2608.23035) | 13 个移动领域、212 个真实工具，联合测试规划、子智能体、记忆和技能 |
| 08-24 | [CDEG](https://arxiv.org/abs/2608.22899) | 对比同病例成功/失败轨迹，用反事实验证提炼诊断关键证据，最高提升 11.5% |
| 08-25 | [The Handoff Tax](https://arxiv.org/abs/2608.24358) | 模型接手非原生长轨迹存在质量税；升级到强模型时减少原模型轨迹反而可能更好 |
| 08-27 | [Behavior2Trip](https://arxiv.org/abs/2608.26807) | 11,400 个旅行规划实例，平均 39.8 条用户行为，显示真实偏好来自行为轨迹而非显式陈述 |

---

## 四、技术产品现状：研究正在如何落地

### 4.1 产品能力矩阵

| 产品/框架 | 已公开能力 | 对应研究路线 | 当前边界或风险 |
|---|---|---|---|
| Claude Managed Agents | 持久 session、事件流实时 steering、sandbox checkpoint、会话预算、定时部署；memory store 以目录挂载，修改产生不可变版本 | PILOT 的实时控制面、event sourcing、版本化记忆 | Managed Agents 与 memory store 仍是 beta；sandbox 只保留 30 天；公开文档未承诺自动语义治理 |
| OpenAI Agents SDK | SQLite/Redis/SQL/Mongo/Dapr/OpenAI 托管 sessions；Responses compaction wrapper 带失败恢复；RunState 可序列化中断与审批状态；Sandbox Agent Memory 为 beta | 会话持久化、检查点、压缩事务边界、跨 run 经验 | Session 主要保存消息历史；长期语义记忆和治理策略仍需应用定义 |
| Codex | 远程查看、回答问题、审批与改变方向；Hooks 可验证、记录或创建记忆；Automations 可周期运行 | 长任务 steering、监督、持续任务 | 面向产品工作流而非通用记忆 API；效果依赖 repo artifact 与任务拆分质量 |
| LangGraph | 每步 graph state checkpoint、线程恢复、time travel、fault tolerance；跨线程 Store 保存 JSON memory | 显式状态、事件检查点、短期/长期分离 | 提供基础设施，不替应用决定写什么、何时冲突、怎样压缩 |
| Letta | 可编辑且常驻上下文的 memory blocks、archival/recall memory、无限消息历史、块共享与动态挂卸、压缩统计 | 分层记忆、agentic context engineering | Agent 自写 block 可能误更新；统一 compaction 仍需防止约束丢失 |
| Mem0 Platform/OSS | 新 OSS 算法采用 ADD-only 抽取、语义+BM25+实体混合检索；Platform 有时间推理、访问衰减、实体作用域、导出 | 事实记忆、混合召回、时间意图、使用反馈 | 官方页面存在版本冲突：最新迁移文档称新 OSS 移除 Graph Memory，但旧功能页/对比页仍展示支持，集成前必须锁定版本实测 |
| Zep / Graphiti | 双时态 Context Graph、episode provenance、实时增量更新、语义+全文+图混合搜索；Zep 提供治理型 Context Lake | 时间关系、变化历史、跨源上下文 | 强项是语义记忆平面，不替代执行状态、原始工具日志和实时监督 |
| Mastra Observational Memory / Gateway | 将历史压缩为 observations，保持稳定上下文；OpenAI-compatible Gateway 按 thread/resource 自动注入 | 观测式摘要、稳定上下文 | 本质仍是有损派生视图；安全约束和原始证据需要独立保留 |

### 4.2 官方资料核验

- **Claude Managed Agents**：官方文档说明 session 持久保存会话历史、idle 时 checkpoint 完整 sandbox，用户事件可在运行中 steer；sandbox 状态自创建起保留 30 天。[Sessions](https://platform.claude.com/docs/en/managed-agents/sessions) / [Event stream](https://platform.claude.com/docs/en/managed-agents/events-and-streaming)。Memory store 跨 session 持久存在，以目录挂载给 Agent，每次修改产生不可变版本，支持直接审阅、纠错、归档和删除。[Agent memory](https://platform.claude.com/docs/en/managed-agents/memory)。
- **OpenAI Agents SDK**：官方 Sessions 文档列出本地与生产数据库后端、历史限制、分支、加密与 OpenAI 托管会话，并提供带恢复语义的 Responses compaction wrapper。[Sessions](https://openai.github.io/openai-agents-python/sessions/)。`RunState` 是中断/恢复与 HITL 的持久边界。[Run state](https://openai.github.io/openai-agents-python/ref/run_state/)。Sandbox Agent Memory 明确与消息 Session 分离，用文件沉淀跨 run 经验，但仍是 beta。[Agent memory](https://openai.github.io/openai-agents-python/sandbox/memory/)。
- **Codex**：官方产品说明强调从手机查看长任务、回答问题、审批或改变方向，Hooks 可执行验证、日志和记忆写入。[Work with Codex from anywhere](https://openai.com/index/work-with-codex-from-anywhere/)。
- **LangGraph**：短期记忆是 thread-scoped graph state，checkpointer 在每步保存；长期记忆通过跨 thread namespace store 保存。Persistence 还提供 time travel 和 pending-write 故障恢复。[Memory](https://docs.langchain.com/oss/python/concepts/memory) / [Persistence](https://docs.langchain.com/oss/python/langgraph/persistence)。
- **Letta**：Memory block 是持久、可编辑、始终在上下文中的结构化块，可在多个 Agent 间共享并动态挂卸。[Memory blocks](https://docs.letta.com/tutorials/attaching-detaching-blocks/)；官方 SDK 将其概括为 in-context block + out-of-context archival/recall + perpetual message history。[Python SDK](https://docs.letta.com/api/python)。
- **Mem0**：最新迁移页公开了 ADD-only、混合搜索和 entity linking，并写明新 OSS 算法移除了 graph store；Platform 提供[时间推理](https://docs.mem0.ai/platform/features/temporal-reasoning)、[Memory Decay](https://docs.mem0.ai/platform/features/memory-decay)和[实体作用域](https://docs.mem0.ai/platform/features/entity-scoped-memory)。版本冲突见[新算法迁移页](https://docs.mem0.ai/platform/features/graph-memory)与仍在线的[旧 Graph Memory 页](https://docs.mem0.ai/open-source/features/graph-memory)。
- **Zep/Graphiti**：官方把 Graphiti 定义为实时增量、双时态、带 provenance 的 Context Graph，支持 time/full-text/semantic/graph 混合搜索；Zep 在其上提供 managed Context Lake。[Graphiti overview](https://help.getzep.com/graphiti/getting-started/overview)。
- **Mastra**：Observational Memory 用 observations 替代持续增长的原始消息，并宣称在指定配置下 LongMemEval 为 94.87%；Gateway 通过 thread/resource id 自动注入观察。[发布说明](https://mastra.ai/blog/observational-memory) / [Gateway](https://gateway.mastra.ai/docs)。

### 4.3 产品格局的关键判断

1. **会话运行时与语义记忆平台正在分化**。Claude/OpenAI/LangGraph 强于 session、checkpoint、tool state、HITL；Mem0/Zep 强于跨会话事实与检索。生产架构通常需要两类组件，而不是二选一。
2. **产品已经支持“持续”，但还没有自动解决“正确持续”**。能 resume 不代表恢复了正确状态；能 compact 不代表安全规则还在；能 search 不代表会在自然对话中使用。
3. **版本化文件记忆正在成为实用中间形态**。Claude memory store、OpenAI Sandbox Agent Memory、Codex repo artifacts 都让模型通过文件读写长期知识，易审计、可 diff，但仍需 schema、验证和权限层。
4. **最新产品控制面已明显追上论文**。Claude 的 event steering、Codex 远程转向和 PILOT 的 supervisor-worker 属于同一趋势：长任务必须允许外部主体在运行中改变航向。

---

## 五、统一技术框架：四平面架构

### 5.1 建议架构

#### A. 证据平面（Evidence Plane）

- append-only event log，记录 user/assistant/tool/system 事件；
- 原始 tool output、文件、截图、音频通过 immutable artifact ID 外置；
- 每条事件带 `session_id / task_id / branch_id / actor / event_time / ingest_time / source / checksum`；
- 永不由摘要覆盖，摘要只保存为派生 artifact；
- 支持按地址精确恢复、时间旅行和审计。

#### B. 状态与记忆平面（State & Memory Plane）

- **Execution State**：当前任务阶段、已验证事实、资源、假设、阻塞项；用 schema + patch + validator 事务更新；
- **Semantic Memory**：事实、偏好、实体关系、事件、程序、失败 guard；
- 每条记忆带：

```text
type, scope, lifecycle_state, valid_time, transaction_time,
source_event_ids, confidence, verifier_label, conflict_links,
access_count, last_accessed_at, privacy_class, version
```

- 状态不从向量检索得出；长期记忆不直接替代状态；所有派生对象必须能回到证据平面。

#### C. 上下文平面（Context Plane）

根据请求先做路由，再组装有界工作视图：

| 请求类型 | 优先输入 |
|---|---|
| 继续当前任务 | 当前 execution state + 当前 branch 局部轨迹 |
| 查询历史事实 | 混合检索 + 时间过滤 + 原始证据 |
| 询问变化/冲突 | 新旧版本链 + valid/transaction time + 来源 |
| 长期总结 | 分层聚合 + 覆盖率检查，不用单次 Top-k |
| 高风险行动 | 原样约束 + 当前状态 + 可执行验证 |
| 跨应用 GUI | 当前截图 + 选择恢复的历史截图 + 文本事件摘要 |
| 自然聊天 | memory-need detector 先判断是否应使用记忆 |

预算分配必须按类型和模态：约束原样固定、状态结构化常驻、事实按证据密度选取、情节按需展开、视觉原件按因果效用恢复。

#### D. 控制与治理平面（Control & Governance Plane）

- checkpoint、pause/resume、budget、deadline、retry；
- supervisor/user 可 `observe / steer / approve / abort`；
- verifier 结果持久参与记忆准入、冲突、降权、归档；
- compaction 前进行 Knowledge Triage，压缩失败可回滚；
- 监控 context size、state drift、重复行动、无进展时长、记忆污染和成本曲线。

### 5.2 写入链路

```text
1. 原始事件追加到 Event Log
2. 类型/作用域/隐私分类
3. 生成 execution-state patch → schema 校验 → 原子提交
4. 提取候选长期记忆，初始为 provisional
5. verifier 检查来源、结果、泛化性与失败风险
6. 执行 ADD / LINK / SUPERSEDE / MERGE / REJECT / ARCHIVE
7. 更新可重建索引（向量、BM25、实体、时间、分支）
```

关键不变量：**任何 derived memory 都不能成为唯一证据；任何状态覆盖都必须保留前版本；任何高风险约束都不能经过自由摘要。**

### 5.3 读取链路

```text
1. 判断：continuation / recall / temporal / aggregation / audit / action
2. 定位当前 session/task/branch 与有效权限域
3. 加载 execution state 与不可压缩约束
4. 混合检索候选，并沿互补关系小范围扩展
5. 去冲突、去冗余、验证来源与时间有效性
6. 在 token/模态预算下生成 evidence plan
7. 注入工作视图，输出时附带 evidence IDs
8. 记录哪些记忆实际影响了决策，形成 retrieval experience
```

---

## 六、评测体系：从记忆分数到持续工作能力

### 6.1 建议的六层评测

| 层 | 核心问题 | 指标建议 | 可复用数据 |
|---|---|---|---|
| 事实召回 | 找得到明确历史信息吗 | answer accuracy、Recall@k、证据覆盖 | LongMemEval、LoCoMo、BEAM |
| 自然整合 | 用户没直接提问时会用对吗 | Natural Integration、满意度、错误主动回忆率 | MemUse |
| 状态连续性 | 外部状态变化后能立即恢复吗 | recovery steps、state exactness、重复动作率 | SkillExecBench、τ-Bench |
| 轨迹连续性 | 能恢复正确任务线吗 | branch localization、confounder rate、handoff loss | BranchMemEval、自建多项目会话 |
| 长程行动 | 早期行动能否改善后期结果 | task success、行为曲线、终局意识、信用分配 | Terminal-Bench、LOCA、FM-Bench |
| 安全与治理 | 压缩/积累后规则和记忆仍可靠吗 | constraint survival、false admission、conflict/stale rate、rollback success | Compaction Cliff、MemGuard 式任务流 |

### 6.2 必须同时报告的成本指标

- 每步平均输入 tokens 与整个 episode 累计 tokens；
- 写入每 turn 的 LLM 调用与延迟；
- 查询准备、TTFT、完整查询延迟；
- active context、长期 store、原始 artifact 三层存储量；
- compaction 频率、恢复失败率和阻塞 streaming 的时间；
- 每个成功任务的 token、美元和 wall time；
- 随 horizon 增长的曲线，而不是只报告一个固定长度点。

### 6.3 对 LongMemEval 的重新定位

LongMemEval 仍适合评估跨会话事实、时间和更新推理，但不应再作为唯一北极星：

- 它主要是固定历史后的 Direct QA；
- 不能测自然使用记忆的时机；
- 不能测错误写入对未来数百任务的污染；
- 不能测中途 steering、checkpoint 和真实环境状态变化；
- 高分可能来自更强 reader、查询扩展或评测提示，而非更好的生命周期设计。

建议把它降级为**证据召回/推理层的单项测试**，并与 MemUse、状态任务、长程行动和 compaction/governance 测试联合。

---

## 七、面向当前技术栈的实施建议

### 7.1 三个优先级

#### P0：先拆开 History、State、Memory

如果当前 Hermes Agent / Mem0 链路仍以“消息 → 抽取事实 → 检索注入”为主，第一优先级不是继续调 embedding，而是增加：

1. append-only 原始事件表；
2. 独立的 execution state schema 与 patch validator；
3. memory 记录到原始 event 的反向证据链接；
4. active context 组装日志，记录每次到底给模型看了什么。

#### P1：把关系判断升级为生命周期治理

现有 `ADD / REPLACE / MERGE / NONE` 关系可以进一步扩为：

- `provisional → active → superseded/merged/archived/rejected`；
- 写入时保存 verifier quality 与 uncertainty；
- 失败经验单独进入 `failure_guard`，不能作为成功动作模板；
- `valid_time` 与 `transaction_time` 分离；
- 检索结果记录实际使用和结果反馈，为 EARM/Memory Decay 式排序提供数据。

#### P2：为不同场景建立不同上下文策略

- LongMemEval 问答：时间/关系感知 evidence plan；
- 连续工具任务：SKILL.state 式结构化状态；
- 多项目聊天：ArborMem 式 branch localization；
- 高风险规则：Knowledge Triage exact pinning；
- 多模态 GUI：摘要常驻 + 历史截图按需恢复；
- 超长开放任务：event log + checkpoint + supervisor steering。

### 7.2 建议的 90 天路线

| 阶段 | 目标 | 交付物 | 成功标准 |
|---|---|---|---|
| 0–30 天 | 建立可观测底座 | Event Log、state schema、evidence IDs、context trace | 任一答案/动作可回溯到状态与原始事件 |
| 31–60 天 | 建立治理型记忆 | provisional/active 生命周期、verifier metadata、冲突版本链、typed compaction | 错误记忆准入率、约束压缩丢失率可量化并下降 |
| 61–90 天 | 建立场景化上下文与长程评测 | branch router、evidence planner、实时 steering、联合 benchmark | 在固定成本下同时提升 LongMemEval、状态任务和自然整合，不只提升单一分数 |

### 7.3 最小可行实验

用同一 backbone 比较四种运行时，避免把模型能力误当成记忆能力：

1. **Baseline A**：最近窗口；
2. **Baseline B**：最近窗口 + 通用摘要；
3. **Baseline C**：Mem0 检索记忆；
4. **Target**：Event Log + explicit state + governed memory + query-time context。

每种运行时跑：

- LongMemEval 500 题；
- 自建 100 个 100–200 步状态任务，含外部状态纠正和 20/50 条噪声；
- 50 条三分支、可中断恢复的会话；
- 20 个真实用户、至少 4 周的自然使用 pilot，记录 memory moments 与满意度；
- 五轮 compaction，混入约束/程序/事实/日志，测原样规则存活率；
- 一个 200+ task 的连续任务流，测错误记忆污染与治理效果。

只有当 Target 在**任务成功、自然整合、状态恢复、约束保存和每成功任务成本**上同时改善，才算真正解决超长时间会话。

---

## 八、风险、反例与开放问题

1. **写入时无法知道未来价值**：Scroll 选择保留原始日志，但查询时计算更重；预提取系统查询快，但可能永久丢细节。理想系统应同时保留原始证据与可重建派生索引。
2. **显式状态不是万能摘要**：SKILL.state 只在 state 是充分统计量时成立。开放研究、创作、审计和未知任务仍需要历史与 artifact。
3. **更主动不一定更好**：MemUse 的主动回忆没有稳定满意度收益，错误时机反而显著伤害体验。
4. **治理依赖 verifier**：MemGuard 和 Knowledge Triage 都把风险前移到分类器/verifier；必须保留 provisional、人工复核、回滚和来源证据。
5. **长程基准可能被单一世界过拟合**：FM-Bench 的行为洞察重要，但需要更多领域、seed 和真实人类管理任务验证。
6. **模型切换会产生 Handoff Tax**：长任务中“换更强模型继续”并不自动恢复质量，交接包应传当前状态、关键证据和未决项，而不是完整原模型轨迹。
7. **多模态存储成本会成为新瓶颈**：截图、视频和工具产物不能都进模型，但删除又不可恢复；需要生命周期、分辨率和访问频率联合治理。
8. **隐私和删除权仍明显不足**：长期记忆一旦跨 session、Agent 和设备共享，需要 consent、purpose binding、字段级加密、删除传播和审计版本的平衡。

---

## 九、最终判断

本轮论文最清晰的共同方向是：

> **用不可变历史保证不丢，用显式状态保证当前正确，用治理型记忆保证长期复用，用按需工作视图保证模型负担有界，用实时控制面保证长任务可纠偏。**

这意味着一个面向 2027 的超长时间会话系统，核心接口不应只是 `memory.add()` / `memory.search()`，而应同时提供：

```text
event.append()        # 保存发生过什么
state.patch()         # 原子更新现在是什么
memory.propose()      # 提交可能有用的长期经验
memory.govern()       # 校验、冲突、版本与归档
context.plan()        # 为当前调用生成有界工作视图
run.checkpoint()      # 中断与恢复
run.steer()/abort()   # 运行中控制
evidence.trace()      # 解释这次行动依据了什么
```

如果只继续优化向量召回，可能还能提高若干 LongMemEval 分数；如果目标是让智能体可靠地工作数小时、数月甚至跨越数千个任务，则必须把问题升级为**会话操作系统与记忆治理系统的联合设计**。

---

## 附录：信息源与检索说明

- arXiv 检索窗口：2026-08-04—2026-08-29；查询包含 `agent memory`、`long-running agent`、`long-term conversational memory` 等，并对本文十篇重点论文读取全文而非只使用摘要。
- 产品能力只采用厂商官方文档、官方发布页和开源项目官方页面；访问日期均为 2026-08-29。
- 论文为预印本时，结果应视为作者报告；除论文内部对照外，本文不把不同 reader、judge、子集或预算下的分数直接比较。
- Mem0 官方资料在 Graph Memory 是否属于新 OSS 算法上存在页面版本冲突，本文按最新迁移页优先，并显式保留冲突说明。
