# Agent 记忆模块全景与最新进展

> **报告日期**：2026 年 9 月 3 日
>
> **重点增量**：2026 年 8 月 4 日—9 月 3 日
>
> **覆盖范围**：开源项目、Benchmark、论文、云厂商与商业产品
> **资料口径**：优先使用论文正文、官方 GitHub 仓库和官方产品文档；产品或作者自报成绩均单独标识

---

## 一、执行摘要

### 1.1 核心判断

Agent 记忆正在从“把历史向量化后检索”升级为一套完整的**状态与知识生命周期系统**：

```text
不可变原始事件流
  ↓ 语义分段 / 价值判断 / 类型路由
多视图记忆：Profile + Event + Record + Procedure
  ↓ 向量、BM25、实体、时间、文件层级等多路索引
查询意图识别 → 动态检索预算 → 证据组合
  ↓
带来源的回答 / 冲突仲裁 / 必要时拒答
  ↓
版本演化、遗忘、删除传播、审计与评测
```

2026 年 8 月的新工作进一步证明：决定效果的已经不只是 embedding 或 top-k，而是以下五个系统问题：

1. **写入粒度**：逐轮提取成本过高，固定窗口又破坏语义边界；语义段正在成为更合适的合并单元。
2. **表示分型**：稳定偏好、持续变化的事件、必须保真的原始记录、可复用流程，不能用同一种摘要策略处理。
3. **可恢复性**：任何写入时摘要都会误判未来价值，因此原始事件日志必须保留，并能按稳定地址回溯。
4. **证据纪律**：高分系统越来越强调 provenance、citation lock、置信度和 abstention，而不是只让模型“相信检索结果”。
5. **生命周期治理**：冲突、过期、隐私删除、派生摘要残留和投毒，开始成为与召回准确率同等级的问题。

### 1.2 本月最值得关注的进展

| 日期 | 工作 | 关键变化 | 代表结果 |
|---|---|---|---|
| 8 月 4 日 | [LeanMem](https://arxiv.org/abs/2608.03463) | 按可压缩性与保真需求路由到 profile / event / record，只演化 event | GPT-4.1-mini 下 LongMemEval-S 91.80，LoCoMo 84.87；论文内同口径对比显著降本 |
| 8 月 13 日 | [LycheeMemory V2](https://arxiv.org/abs/2608.12990) | 用语义段替代逐轮合并，typed record + 多路检索 | LongMemEval-S 92.20，LoCoMo 89.22；相对 A-Mem 构建 token 分别下降 75.9% / 86.0% |
| 8 月 21 日 | [Scroll](https://arxiv.org/abs/2608.21690) | 把上下文变成可编程环境：append-only Event Log + 持久 Python namespace | LongMemEval-S 94.8，BEAM-10M 73.1，LOCA-256K 86.7 |
| 8 月 26 日 | [MemToC](https://arxiv.org/abs/2608.26295) | 首次控制模型记忆与工具返回各自真伪，评测冲突仲裁 | 错误工具面前，模型保留自身正确答案仅 6.5%–17.1% |
| 8 月 30 日 | [Agent Zero Memory](https://arxiv.org/abs/2608.29606) | timeline + entity-event graph + hierarchical documentary memory，所有回答执行 citation lock | 论文自报 LongMemEval 95.60、LoCoMo 93.60 |

### 1.3 对现有 Mem0 路线的直接结论

Mem0 在 2026 年 4 月的平台算法中采用 ADD-only 提取、语义 + BM25 + entity 多信号召回和时间推理，并自报 LongMemEval 94.4、LoCoMo 92.5；但官方仓库明确说明这是**托管平台的专有优化成绩**，不等同于开源 SDK。[Mem0 官方仓库](https://github.com/mem0ai/mem0)

ADD-only 的价值是避免错误 UPDATE/DELETE 破坏历史，但它只解决了“不要破坏旧事实”，没有完成：

- 当前态与历史态的显式分离；
- 原始证据到派生事实的完整 lineage；
- 查询阶段的冲突仲裁和拒答；
- 删除请求向摘要、图边、缓存和索引的级联传播；
- 程序性经验与事实性记忆的分轨管理。

因此，下一阶段不应继续只调 embedding 或 top-k，而应在 Mem0 之上补齐**事件溯源、分型写入、检索后证据组织、双时态版本和删除闭环**。

---

## 二、Agent 记忆的技术栈已经分成六层

| 层 | 主要职责 | 典型实现 | 当前前沿 |
|---|---|---|---|
| 工作记忆 / 会话状态 | 保持当前任务、工具结果与短期上下文 | message history、checkpoint、compaction | 从“完整重放”转向可恢复的 working view |
| 摄取与筛选 | 判断何时记、记什么、是否需要原文 | fact extraction、salience、topic segmentation | 语义段写入、低价值过滤、后台批处理 |
| 长期表示 | 保存事实、事件、偏好、经历、技能 | 向量、图、Markdown、关系表 | profile / event / record / procedure 多视图并存 |
| 演化与治理 | 合并、冲突、取代、过期、遗忘、删除 | ADD/UPDATE/DELETE、valid interval、tombstone | 非破坏版本化、来源置信度、全链路删除 |
| 检索与组装 | 找到并组织当前问题真正需要的证据 | dense、BM25、graph、time、agentic search | intent gate、query planner、动态预算、证据链 |
| 评测与运维 | 衡量准确率、成本、延迟、安全与健康度 | LongMemEval、LoCoMo、trace、metrics | 端到端与 retrieval 分离；新增隐私、流程、环境经验评测 |

一个重要边界是：**Conversation State 不等于 Long-Term Memory**。例如 OpenAI Responses API 的 `conversation` 或 `previous_response_id` 能延续多轮状态，但只是会话链路；如果产品需要跨会话提炼用户偏好、事实更新或程序经验，仍需独立的记忆摄取、演化和检索层。[OpenAI Responses API](https://developers.openai.com/api/reference/cli/resources/responses/methods/create)

---

## 三、最新论文与研究方向

### 3.1 LeanMem：不同信息采用不同压缩策略

[LeanMem](https://arxiv.org/abs/2608.03463) 的核心不是新的检索器，而是把记忆写入建模为“在可接受信息损失约束下选择最低成本表示”：

- `ignore`：无持久价值的临时内容；
- `profile`：稳定属性与偏好，直接结构化；
- `event`：会随时间变化的状态，保留时间锚点并增量演化；
- `record`：列表、方案、细节密集内容，仅生成检索 gist，保留原文指针。

它只对 event memory 做延迟、局部合并，避免稳定 profile 和不可变 record 被反复总结。查询时再根据证据粒度、时间性和保真度分配不同记忆类型的检索预算。

**可落地启示**：写入时最危险的不是漏掉“重要事实”，而是错误判断信息可以被压缩。凡是未来查询形态不可预知、细节密集或需审计的内容，应该保存 source pointer，而不是只保存抽取事实。

### 3.2 LycheeMemory V2：语义段是更好的合并粒度

[LycheeMemory V2](https://arxiv.org/abs/2608.12990) 用 embedding 驱动的语义边界检测，把多个 exchange 合并成 300–900 token 的连贯片段，再调用一次 LLM 编码 typed records。其记录包含 fact、preference、event、constraint、procedure、failure pattern、tool affordance，以及实体、标签、时间范围和来源 turn range。

关键实验结果：

- GPT-4.1-mini 下 LoCoMo 89.22、LongMemEval-S 92.20；
- 相比 A-Mem，LoCoMo 构建 token 从 1459.9K 降到 204.1K，LongMemEval-S 从 1264.3K 降到 304.7K；
- 取消语义分段改为固定窗口，LoCoMo 准确率从 89.22 降到 82.40；
- 只用 record vector retrieval，准确率降至 81.75；
- 去掉 fusion、reranking 和 diversity selection，准确率降至 66.62。

**可落地启示**：批处理负责降成本，语义边界负责保质量，多路召回后的选择栈负责最终准确率。三者不能互相替代。

### 3.3 Scroll：上下文不是文本，而是可编程环境

[Scroll / Context as an Environment](https://arxiv.org/abs/2608.21690) 代表了一条区别于传统 memory extraction 的路线：

- 全量历史进入 append-only Event Log，并获得稳定序号；
- 大工具输出外置为可恢复 payload；
- 持久 Python kernel 在多次模型调用间保留 typed variables；
- 模型通过 `search / expand / compute / print` 构造下一次调用的 working view；
- 旧 span 被 eviction，而不是被不可逆 summary；eviction index 保留可导航地标。

这等于把“记忆选择”从写入时推迟到查询时，让模型对原始历史执行程序化过滤、连接、聚合和状态解析。其代价是更复杂的 sandbox、状态隔离和工具安全，但对 10M token 级长任务很有吸引力。

**可落地启示**：对 coding agent、deep research 和长工作流，保存事实卡片还不够；工具输出、变量、artifact 和中间计算状态应作为可寻址对象存在，而不是每次重新序列化进 prompt。

### 3.4 Agent Zero Memory：多表示 + 来源锁

[Agent Zero Memory](https://arxiv.org/abs/2608.29606) 同时维护三套互补视图：

1. Memory Events timeline：负责“何时发生、何时改变”；
2. Entity–Event Graph：负责跨会话实体与事件关联；
3. Hierarchical Documentary Memory：负责稳定、可读、可引用的长期事实。

查询先经过 intent gate 和 source router，再并发运行三条 agentic search；每条搜索使用 dense + lexical，并由 agent 自主添加时间、来源、说话人等过滤条件。回答只能引用本次实际打开过的证据，即 citation lock；无法形成证据集合时应拒答。

论文自报 LongMemEval 95.60、LoCoMo 93.60，并显示不同 backbone 之间 LongMemEval 只相差 3.4 分，而每查询成本相差约 30 倍。这个结果支持“高质量记忆主要由表示与检索驱动，而不是只由答案模型驱动”的判断。

但应注意：论文也承认竞品分数来自各自最佳公开配置，judge、prompt、retrieval budget 并未统一；该横向表只能看作强结果定位，不能视作严格 head-to-head。

### 3.5 文件系统记忆：可读可改，但不会自动保持健康

[Filesystem-Based Memory for LLM Agents](https://arxiv.org/abs/2607.26637) 系统研究了 Markdown 目录树式记忆。结论比“文件优于向量库”更克制：

- 大规模材料下，有组织的文件树能把检索成本大致减半；
- 组织本身没有稳定提升回答正确率；
- 随着记忆增长，除最强管理模型外，所有 agent 的分类结构都会退化；
- 只改变工具集合，就能像更换模型一样显著改变最终目录形态；
- 重组 agent 若没有“保留每个事实”的硬约束，会自然地压缩并丢失细节。

这与 Letta MemFS、ReMe、OpenViking 和 Anthropic Memory Tool 的产品方向形成呼应：文件系统提供可审计、可版本化、可人工修复的控制面，但仍需要健康度检查、来源约束和定期维护策略。

### 3.6 从召回准确率扩展到隐私、安全与仲裁

- [Deployment-Time Memorization](https://arxiv.org/abs/2606.10062) 提出 Personalization Recall、Adversarial Extraction Rate 和 Forgetting Residue Score。实验中，仅删除 raw chunk 会让派生摘要在约 20% 样本中继续暴露已删信息；只有全链路 purge 或 tombstone redaction 才把最坏层残留降为零。
- [MemToC](https://arxiv.org/abs/2608.26295) 证明模型非常容易盲从错误工具：五个 7B–9B 模型中，instruction-tuned 模型在“模型原答案正确、工具错误”时仅 6.5%–17.1% 能坚持正确答案。
- 这意味着 memory result 不能作为高权威 system fact 无条件注入。每条记忆至少需要来源、时间、可信度、当前有效性和冲突状态；工具结果与记忆冲突时应进入显式仲裁，而不是靠 prompt 中一句“优先最新信息”。

---

## 四、开源项目格局

### 4.1 代表项目对比

| 项目 | 核心表示 | 检索 / 演化特点 | 部署形态 | 适合场景 | 主要风险 |
|---|---|---|---|---|---|
| [Mem0](https://github.com/mem0ai/mem0) | 原子事实，可选图 | 新平台算法使用 ADD-only + semantic/BM25/entity + temporal | Apache-2.0 SDK、自托管服务、Cloud | 快速给现有 Agent 加用户记忆 | 平台与 OSS 能力不完全等价；原始证据与版本治理需补强 |
| [Letta / Letta Code](https://github.com/letta-ai/letta-code) | MemFS：Git-backed Markdown，system / skills / external memory 分层 | agent 自主编辑；dreaming 后台反思；worktree 并发维护 | 本地、Server、Cloud / Constellation | 长寿命个人或 coding agent，自我改进 | 文件结构会漂移；热上下文与外部文件的边界需治理 |
| [Graphiti](https://github.com/getzep/graphiti) | 双时态 context graph：entity、fact edge、episode | semantic + BM25 + graph traversal；旧事实失效但不删除 | Apache-2.0，自带多种图后端；Zep 为托管版 | 强时间、关系、历史追踪 | 摄取 LLM 调用多，写放大和图运维成本高 |
| [Cognee](https://github.com/topoteretes/cognee) | 向量 + 知识图 + ontology | 多格式摄取、图推理、持久长期记忆 | Apache-2.0，自托管平台 | 企业资料与 Agent memory 融合 | 数据管线较重；个性化时间演化不是唯一中心 |
| [Hindsight](https://github.com/vectorize-io/hindsight) | world fact、experience、observation、mental model | retain / recall / reflect；semantic/BM25/graph/temporal 四路融合；证据支持的 observation | MIT，自托管、嵌入式、Cloud | 需要“学习”而非只检索的业务 Agent | reflect 与后台合并引入成本；供应商 benchmark 需独立复现 |
| [ReMe](https://github.com/agentscope-ai/ReMe) | Markdown + frontmatter + wikilink | BM25 默认、可选 vector、RRF、line-level recall；Auto Dream 合并 digest | Apache-2.0，本地优先、CLI/HTTP/MCP | 中文生态、个人知识库、多 Agent 共享 | 文件质量依赖维护 agent；官方 LongMemEval 为 agentic pipeline 自报 |
| [OpenViking](https://github.com/volcengine/OpenViking) | `viking://` 虚拟文件系统，resource / memory / skill 统一 | L0 摘要、L1 概览、L2 原文；目录递归检索；会话提交后提取记忆与经验 | AGPL-3.0，自托管、商业托管 | 上下文工程、资源与技能统一管理 | AGPL 许可评估；其 LoCoMo 结果依赖特定模型栈 |
| [Supermemory](https://github.com/supermemoryai/supermemory) | memory graph + profile + document/RAG | 自动冲突与过期；混合搜索；多模态文档与连接器 | MIT，Local、API、Cloud | 一体化个人 / 公司 context engine | “95% LongMemEval”是 Recall@15，不是端到端 QA accuracy |

### 4.2 2026 年开源路线的明显分化

**事实卡片路线**：Mem0、Supermemory。接入简单、查询成本稳定，适合用户画像和客服；难点是错误抽取后不可恢复、冲突链和来源治理。

**时间图路线**：Graphiti/Zep、Cognee。关系与历史查询强，适合企业实体和持续变化事实；代价是写放大、实体消歧和图数据库运维。

**文件 / context repository 路线**：Letta MemFS、ReMe、OpenViking、Anthropic Memory Tool。透明、版本友好、适合 coding agent 与人机共管；但“可编辑”不等于“会正确组织”，需要 memory doctor、dreaming、schema 和 provenance。

**认知分型路线**：Hindsight、LeanMem、Agent Zero。把事实、经历、观察、事件、稳定档案分开，并在 query time 路由；效果强，但系统复杂度与后台 LLM 成本更高。

**可编程上下文路线**：Scroll。直接保留 lossless event log，让模型编程获取和变换历史；特别适合长任务状态，但对 sandbox、trace 与执行可靠性要求最高。

---

## 五、Benchmark 版图与分数口径

### 5.1 主流 Benchmark

| Benchmark | 规模与任务 | 真正测到什么 | 局限与使用建议 |
|---|---|---|---|
| [LoCoMo](https://github.com/snap-research/locomo) | 10 段超长对话；主流评测使用 1,540 个 single-hop、multi-hop、temporal、open-domain QA | 长期人物事实、跨 session 组合、时间推理 | 对话数量少且为生成数据；不同工作对过滤、judge、答案容忍度处理不同 |
| [LongMemEval](https://github.com/xiaowu0162/LongMemEval) | 500 题；S 版约 115K token；覆盖提取、多 session、更新、时间、拒答 | 目前最常用的跨会话端到端 QA | 已接近饱和；cleaned split、reader、judge、top-k 变化都会改变结果 |
| [BEAM](https://github.com/mohammadtavakoli78/BEAM) | 100K / 500K / 1M / 10M；十类记忆问题 | 极长上下文下的扩展性与能力退化 | 成本高；需同时报告构建与查询 token，不宜只报总分 |
| [MemoryAgentBench](https://arxiv.org/abs/2507.05257) | 增量多轮格式；四类能力 | accurate retrieval、test-time learning、long-range understanding、selective forgetting | 更全面，但由多个来源任务转换，子任务指标不宜压成一个排行榜 |
| [LoCoMo-Plus](https://aclanthology.org/2026.acl-long.1150/) | cue 与 trigger 语义不直接匹配 | 隐含目标、价值与约束的 cognitive memory | 比事实召回更接近个性化，但自动评判更难 |
| [EvoMemBench](https://github.com/DSAIL-Memory/EvoMemBench) | 15 种方法；in/cross-episode × knowledge/execution 四象限 | 记忆是否真正带来自我演化 | 结果显示没有通用赢家；强 long-context baseline 仍非常有竞争力 |
| [AFTER](https://arxiv.org/abs/2606.23127) | 382 个企业任务、6 个角色、22 类程序技能 | procedure 的局部提升、跨任务/角色/模型迁移 | 面向执行经验，不应与对话事实 QA 混排 |
| [LongMemEval-V2](https://github.com/xiaowu0162/LongMemEval-V2) | 451 题；最多 500 条多模态 web trajectory、115M token | 环境状态、状态变化、workflow、gotcha、错误前提识别 | 查询延迟很高，但更接近“有经验的同事”而不是聊天记忆 |

### 5.2 LongMemEval-V2 是评测方向的关键转折

LongMemEval-V2 不再问“用户几个月前说了什么”，而是问 Agent 是否从历史轨迹中学会了环境：页面布局、状态转换、可靠工作流、本地陷阱和不成立的默认假设。

官方结果中：

- 简单 RAG 总体约 40.1%；
- AgentRunbook-R 约 57.8%，查询延迟约 26 秒；
- 直接让通用 coding agent 搜历史可达 69.3%，但约 182 秒 / query；
- AgentRunbook-C 达 72.5%，并比通用 coding agent 快 32%。

这说明未来记忆系统要同时优化**答案正确率、查询延迟、证据预算和历史规模**；单一 LongMemEval-S 分数已经不足以代表生产价值。[LongMemEval-V2 论文](https://arxiv.org/abs/2605.12493)

### 5.3 不能放在同一列比较的“分数”

| 类型 | 示例 | 含义 |
|---|---|---|
| Retrieval Recall@K | Supermemory 95% Recall@15；某些项目 R@5/R@10 达 95%+ | 金标准证据是否出现在 top-k，不代表答案模型最终答对 |
| End-to-end QA Accuracy | Agent Zero 95.60、Mem0 Platform 94.4、Lychee 92.20 | 检索 + evidence assembly + reader + judge 的整条链路 |
| Oracle / Full Context | 直接提供金标准 session 或完整历史 | 读者上限或长上下文基线，不是实际检索能力 |
| Task success | AFTER、tau2-bench、ALFWorld、LongMemEval-V2 工作流 | Agent 是否完成动作，更接近业务结果，但不能与对话 QA 互换 |
| Vendor composite score | 自定义加权、不同 judge 或选定子集 | 只能用于同一 harness 内部比较 |

社区项目 [agentmemory V4](https://github.com/JordanMcCann/agentmemory) 自报 LongMemEval 96.2%（481/500），高于近期论文结果；这是值得复现的工程信号，但其 generator、judge、提示词、数据版本和检索条件需独立审计，当前不宜直接认定为统一排行榜世界纪录。

建议每次报告至少固定并公开：dataset commit、cleaned split、问题数量、memory build 输入、retrieval budget、reader、judge、prompt、temperature、随机种子、失败重试、构建 token、查询 token、p50/p95 latency 和每类问题得分。

---

## 六、公司产品与云平台

### 6.1 面向终端用户的记忆

| 产品 | 当前形态 | 关键边界 |
|---|---|---|
| ChatGPT Memories | 跨讨论保留稳定偏好、重复工作流、项目约定等上下文；用户可在设置中管理 | 与必须执行的项目指令不同；强约束仍应放在 `AGENTS.md` 或版本化项目文档。[OpenAI 官方说明](https://learn.chatgpt.com/en/docs/personalize) |
| Letta Cloud / Constellation | Agent 身份、对话和 Git-backed MemFS 跨设备同步；支持 dreaming 后台反思 | 更像“长期存在的 Agent runtime”，而不是给任意模型加一个搜索 API。[Letta](https://github.com/letta-ai/letta-code) |
| Supermemory App | 跨对话、文件和连接器形成个人或公司 context graph | 同时覆盖 RAG 与个性化记忆，平台范围大；需关注数据边界与连接器权限。[Supermemory](https://github.com/supermemoryai/supermemory) |

### 6.2 开发者 Memory API / Managed Memory

| 产品 | 记忆模型 | 谁控制存储 | 成熟度与特点 |
|---|---|---|---|
| [Anthropic Memory Tool](https://platform.claude.com/docs/en/agents-and-tools/tool-use/memory-tool) | Claude 对 `/memories` 执行 view/create/replace/insert/delete/rename | 完全由客户端实现，可映射到文件、DB、云存储或加密存储 | 已从 beta interface 走向通用工具；透明、JIT 读取，但 extraction、权限和多租户隔离由应用负责 |
| [Google Vertex AI Memory Bank](https://docs.cloud.google.com/vertex-ai/generative-ai/docs/agent-engine/memory-bank/set-up) | Agent Engine 的 generate / retrieve memories；可配置哪些信息值得持久化 | Google 托管，IAM 控制；也可从 Cloud Run 等任意 runtime 访问 | 与 Agent Engine Session 集成，适合 GCP 原生应用 |
| [AWS Bedrock AgentCore Memory](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/memory.html) | 短期 raw events + 长期 semantic / summary / user preference / episodic 策略 | AWS 托管，按 actor/session/namespace 隔离 | 策略完整、支持自定义与 KMS；长记忆后台异步 extraction + consolidation |
| [Microsoft Foundry Memory](https://learn.microsoft.com/en-us/azure/foundry/agents/concepts/what-is-memory) | user profile、chat summary、procedural memory | Azure 托管 Memory Store | Public Preview；支持 item CRUD、默认 TTL、remember/forget、冲突合并，当前有区域和配额限制 |
| [Mem0 Platform](https://github.com/mem0ai/mem0) | 用户、session、agent 多级记忆；混合与时间感知召回 | Mem0 托管或自托管 OSS | 接入成本低，公开 benchmark 强；平台专有能力与 OSS 差异必须在选型时确认 |
| [Zep](https://github.com/getzep/graphiti) | 托管 temporal context graph | Zep 托管或客户云；Graphiti 为 OSS core | 强 provenance、时间与关系；适合组织级 context graph |
| [Hindsight Cloud](https://github.com/vectorize-io/hindsight) | retain / recall / reflect + observations / mental models | Cloud、自托管 PostgreSQL/pgvector 或嵌入式 | 同时提供 Agent SDK、LLM wrapper、MCP 与大量集成，偏“会形成观点与经验”的记忆 |

### 6.3 云厂商已经形成共同抽象

AWS、Google 和 Microsoft 的产品正在收敛到相似接口：

```text
scope / actor / user
  ├── session events（短期、原始）
  └── memory records（长期、提炼）
        ├── profile / preference
        ├── summary / semantic fact
        ├── episodic event
        └── procedure

write / ingest → background extract & consolidate → search / retrieve → CRUD / TTL / delete
```

差异主要不在 API 名称，而在：是否保留原始证据、是否支持版本历史、冲突解决是否透明、删除是否覆盖派生层、是否能自托管、是否支持共享组织记忆，以及成本和区域合规。

---

## 七、未来 6–12 个月的技术趋势

### 趋势 1：从单一向量库转向多表示 memory fabric

timeline 解决时间与更新，graph 解决关系与多跳，profile/doc 解决稳定事实，raw record 解决证据保真，procedure 解决技能迁移。单一表示仍可作为轻量起点，但不再是高可靠系统的终态。

### 趋势 2：语义段替代逐轮写入

逐轮 LLM extraction 成本与噪声都高。语义段能共享一次编码调用、保留事件边界，并自然生成 provenance turn range。工程上应把 segmentation 作为独立可评测组件。

### 趋势 3：写入时少做不可逆决定

LeanMem 的 record pointer、Scroll 的 Event Log、Agent Zero 的 raw artifacts 都指向同一原则：保留原文，派生层可重建。摘要和事实卡应该是 materialized view，而不是唯一真相源。

### 趋势 4：查询规划成为新瓶颈

当底层同时有 profile、event、graph、files、skills 时，问题变成“这次该查哪一层、多少条、是否需要时间过滤、是否需要多跳”。intent gate、source router、query planner 和 evidence budget 将取代固定 top-k。

### 趋势 5：程序性记忆与事实性记忆分轨

LongMemEval-V2、EvoMemBench 和 AFTER 都表明“知道事实”与“学会完成任务”不是一个问题。程序性记忆需要记录前置条件、动作序列、工具 affordance、失败模式和适用范围，还要测试跨任务、跨角色和跨模型迁移。

### 趋势 6：记忆本身成为安全边界

未来生产系统需要像数据库一样支持：tenant isolation、ACL、source trust、KMS、审计、tombstone、派生数据 lineage、全链路删除、注入检测和 memory poisoning 测试。只提供 `add/search/delete` 已经不够。

### 趋势 7：Benchmark 从静态 QA 走向在线成长

LoCoMo/LongMemEval-S 仍适合回归测试，但新 benchmark 更关注增量学习、环境经验、程序迁移、长期规模、隐式约束、遗忘和隐私。未来榜单会从一个 accuracy 数字变成 accuracy–cost–latency–safety Pareto frontier。

---

## 八、建议的工程路线

### 8.1 推荐目标架构

```text
                         ┌──────────────────────┐
messages / files / tools │ Immutable Event Log  │
────────────────────────>│ source_id + time + ACL│
                         └──────────┬───────────┘
                                    │ semantic segmentation
                                    ▼
                         ┌──────────────────────┐
                         │ Memory Write Router  │
                         │ ignore/profile/event │
                         │ record/procedure     │
                         └───┬────┬────┬────┬───┘
                             │    │    │    │
                      Profile  Event  Record  Procedure
                         KV   bitemporal pointer skill/workflow
                             timeline  to raw
                             └────┬────┘
                                  ▼
               vector + BM25 + entity + temporal + file hierarchy
                                  │
query → intent gate → route & budget planner → retrieve / rerank / diversify
                                  │
                        evidence graph / trace
                                  │
                      citation lock + confidence
                                  │
                          answer or abstain
```

### 8.2 分阶段落地

**P0：先把分数变得可信（1–2 周）**

- 固定 LongMemEval cleaned-S 数据版本、500 题和 judge；
- 分开记录 retrieval recall 与 end-to-end accuracy；
- 输出每类问题、构建 token、查询 token、p50/p95 latency；
- 每条 memory 增加 `source_id / source_span / event_time / created_at / actor / scope`；
- 建立 immutable raw event store，派生 memory 可全量重建。

**P1：提升准确率与成本效率（2–4 周）**

- 把逐 turn extraction 改为 semantic segment batching；
- 引入 profile / event / record 三路写入；
- record 只存 gist + keyword/entity + raw pointer；
- 使用 vector + BM25 + entity/time filters，多路 RRF 后 rerank；
- 在答案模型前加入证据去重、时间排序、冲突分组和 token budget。

**P2：补齐冲突与生命周期（4–8 周）**

- event 使用 `valid_from / valid_to / transaction_from / transaction_to`；
- UPDATE 实际写新版本并关闭旧版本有效区间，不物理覆盖；
- 维护 `supersedes / supports / contradicts / derived_from`；
- 删除沿 raw、fact、summary、graph、embedding、cache 全链路传播；
- 引入 tombstone，测 Forgetting Residue Score。

**P3：从聊天记忆走向 Agent 经验（8–12 周）**

- 从成功与失败轨迹提取 procedure、precondition、failure pattern、tool affordance；
- 用 AFTER / EvoMemBench 测跨任务迁移；
- 用 LongMemEval-V2 测环境状态、workflow 和 gotcha；
- 对超长工具轨迹试验 Scroll 式 event log + programmatic context，而不是继续扩大 prompt。

### 8.3 推荐 KPI

| 维度 | 指标 |
|---|---|
| 答案 | End-to-end accuracy；按 preference / update / temporal / multi-session / abstention 分桶 |
| 检索 | gold evidence Recall@K、MRR、NDCG、证据覆盖率、冲突版本同时召回率 |
| 可信 | provenance coverage、citation precision、unsupported claim rate、abstention precision |
| 演化 | stale-answer rate、supersede accuracy、错误合并率、历史态查询准确率 |
| 遗忘 | deletion latency、派生层残留率、Forgetting Residue Score |
| 成本 | build tokens / session、query tokens / question、LLM calls、存储增长率 |
| 性能 | ingest throughput、query p50/p95、后台 consolidation backlog |
| 安全 | cross-tenant leakage、memory poisoning success rate、PII/secret retention rate |

---

## 九、选型建议

| 需求 | 推荐起点 | 需要补的能力 |
|---|---|---|
| 快速给客服 / 助手增加用户偏好 | Mem0 OSS/Platform、AWS AgentCore、Microsoft Foundry | 数据版本、删除闭环、独立 benchmark |
| 强时间更新与实体关系 | Graphiti/Zep | 控制写入成本，保留 raw source，监控图漂移 |
| Coding Agent / 长期项目 Agent | Letta MemFS、ReMe、OpenViking、Anthropic Memory Tool | schema、memory doctor、来源引用、增长治理 |
| 需要观察、观点和深层反思 | Hindsight | 限制 reflect 成本，审计 belief 的证据和更新 |
| 超长工具轨迹与 1M–10M token 历史 | Scroll 式 Event Log + programmatic context | 安全执行、状态隔离、失败恢复和可观测性 |
| 目标超越 Mem0 94.4 且可解释 | immutable event log + LeanMem 分型 + Agent Zero 证据锁 + 双时态事件 | 统一 harness 下做 ablation，而不是直接拼装全部组件 |

最值得优先做的组合不是“最复杂的全部模块”，而是：

> **语义段写入 + profile/event/record 分型 + raw pointer + hybrid retrieval + 时间版本 + citation lock**。

这组能力同时覆盖当前 LongMemEval 的主要失分项，也为后续程序性记忆、隐私删除和组织共享留下正确的数据基础。

---

## 十、结论

截至 2026 年 9 月，Agent 记忆已经出现清晰的工程共识：

- 原始历史应作为不可变真相源，摘要和事实是可重建视图；
- 不同信息类型需要不同压缩、演化和检索策略；
- 多路召回只是第一步，检索后证据规划、冲突处理与来源约束决定端到端质量；
- 事实记忆与程序经验必须分开评测；
- 删除、投毒、权限与 provenance 是记忆系统的数据库级职责；
- LongMemEval-S 仍有价值，但单一高分已无法证明生产级记忆能力。

下一阶段的竞争，不会是谁的向量库多召回两条，而是谁能在多年、多人、多来源和多 Agent 环境中，持续维护一个**可恢复、可验证、可演化、可删除且成本可控的状态模型**。

---

## 主要资料

### 论文与 Benchmark

- [Agent Zero Memory](https://arxiv.org/abs/2608.29606)
- [MemToC](https://arxiv.org/abs/2608.26295)
- [Context as an Environment / Scroll](https://arxiv.org/abs/2608.21690)
- [LycheeMemory V2](https://arxiv.org/abs/2608.12990)
- [LeanMem](https://arxiv.org/abs/2608.03463)
- [Filesystem-Based Memory for LLM Agents](https://arxiv.org/abs/2607.26637)
- [LongMemEval-V2](https://arxiv.org/abs/2605.12493)
- [EvoMemBench](https://arxiv.org/abs/2605.18421)
- [Managing Procedural Memory / AFTER](https://arxiv.org/abs/2606.23127)
- [Deployment-Time Memorization](https://arxiv.org/abs/2606.10062)
- [MemoryAgentBench](https://arxiv.org/abs/2507.05257)
- [LoCoMo-Plus](https://aclanthology.org/2026.acl-long.1150/)

### 官方项目与产品

- [Mem0](https://github.com/mem0ai/mem0)
- [Letta Code](https://github.com/letta-ai/letta-code)
- [Graphiti](https://github.com/getzep/graphiti)
- [Cognee](https://github.com/topoteretes/cognee)
- [Hindsight](https://github.com/vectorize-io/hindsight)
- [ReMe](https://github.com/agentscope-ai/ReMe)
- [OpenViking](https://github.com/volcengine/OpenViking)
- [Supermemory](https://github.com/supermemoryai/supermemory)
- [OpenAI：ChatGPT 个性化与 Memories](https://learn.chatgpt.com/en/docs/personalize)
- [Anthropic Memory Tool](https://platform.claude.com/docs/en/agents-and-tools/tool-use/memory-tool)
- [Google Vertex AI Memory Bank](https://docs.cloud.google.com/vertex-ai/generative-ai/docs/agent-engine/memory-bank/set-up)
- [AWS Bedrock AgentCore Memory](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/memory.html)
- [Microsoft Foundry Memory](https://learn.microsoft.com/en-us/azure/foundry/agents/concepts/what-is-memory)
